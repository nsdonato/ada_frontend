/*-----------------------------  INFO CLASE -------------------------------------------------------------

Funciones en javascript.
Callstack:
	first in, first out. muestra como se ve en la consola del desarrolador. como se llaman una a otra y cuando se termina su scope.

Callbacks:
	func x param

	ej con setTimeout


/*-----------------------------  EJERCICIOS -------------------------------------------------------------
*/
// falta que male pase el ejercicio..
let password = 12;

const cumpleCaracteres = () => {
	return password.length > 6;
}
const esPasswordValido = () => {
	//if (cumpleCaracteres && 
}
